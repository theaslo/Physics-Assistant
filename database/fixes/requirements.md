# Database Fixing
- User wants to fix all the features of the parent directory database.
- Make a `plan.md    file to complete this fixing task.
- User understands modification to docker-compose.project.yml file. 
- User wants to keep everything dockerized 
    - Make all code in `database`
- Remember to grade subagents and tools needed and if score below 9.0, create new subagent.
-Make sure everything will be able to run with `docker-compose.production.yml` including UI, mcp servers and database.
- Read the `requirements.md` and `plan.md` file in the parent folder `database` to understand the project.
- Create a `memory.md` file for everything done in order to continue working in case of interruptions.